QuantCam icons + manifest package (correct folder name).

Install:
1) Upload 'manifest.json' to repo root (replace existing).
2) Upload folder 'icons/' to repo root (must be exactly 'icons').
3) (Optional) Upload 'favicon.ico' to repo root.
4) Commit. On phone: uninstall old shortcut, clear site data, open site, Add to Home Screen.
